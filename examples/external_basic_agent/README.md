# External Basic Agent

This project demonstrates the preview `sulcus-agent.toml` manifest format for an
external basic Python agent. Sulcus can validate the project, but external
agent execution is not supported yet.
